{
  "name": "@repo/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "studio-agent": "./dist/agent.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "esbuild src/cli.ts --bundle --platform=node --format=esm --outfile=dist/agent.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@repo/shared": "workspace:*"
  }
}
